# ============================================
# UV Market School - Railway Environment Variables
# ============================================
# Copy this content to Railway Dashboard > Variables
# Or use: railway variables set KEY=VALUE

# Authentication (Required)
# ------------------------------------------------------------------------------
# NEXTAUTH_URL: The canonical URL of your deployed application
# Replace {{RAILWAY_DOMAIN}} with your actual Railway domain
# Format: https://your-project.railway.app or your custom domain
NEXTAUTH_URL=https://your-project.railway.app

# NEXTAUTH_SECRET: Secret key for JWT encryption
# Generate with: openssl rand -base64 32
# This MUST be set and kept secure in production
# ⚠️ Generate a new secret for production!
NEXTAUTH_SECRET=your-super-secret-jwt-key-change-this-in-production

# Database - PostgreSQL (Railway Production)
# ------------------------------------------------------------------------------
# Railway provides DATABASE_URL automatically when you add PostgreSQL service
# No need to set manually - Railway injects this environment variable
# Format: postgres://username:password@hostname:port/database
# DATABASE_URL=postgres://... (Auto-injected by Railway)

# Optional: If you need to connect to a different database
# DATABASE_URL=

# Razorpay Payments (Optional - Set to enable payments)
# ------------------------------------------------------------------------------
# Only required if using Razorpay payment integration
# Get these from Razorpay Dashboard > API Keys
# RAZORPAY_KEY_ID=
# RAZORPAY_KEY_SECRET=

# App Configuration (Optional)
# ------------------------------------------------------------------------------
# App Name displayed in browser tab and PWA
# NEXTAUTH_APP_NAME=UV Market School

# Node Environment (Set automatically by Railway)
# ------------------------------------------------------------------------------
# NODE_ENV=production (Auto-set by Railway)

# Logging Level (Optional)
# ------------------------------------------------------------------------------
# Set to 'debug' for verbose logging, 'error' for errors only
# LOG_LEVEL=error

# Rate Limiting (Optional)
# ------------------------------------------------------------------------------
# API rate limit per IP per minute
# RATE_LIMIT_MAX=60

# Session Configuration (Optional)
# ------------------------------------------------------------------------------
# Session timeout in seconds (default: 30 days)
# SESSION_TIMEOUT=2592000

# ============================================
# RAILWAY DEPLOYMENT NOTES
# ============================================
# 1. Add PostgreSQL service in Railway dashboard
# 2. Copy these variables to Railway dashboard
# 3. Set NEXTAUTH_URL to your Railway domain
# 4. Generate a secure NEXTAUTH_SECRET
# 5. Deploy and run database migrations
# 6. Seed the database with initial data
# 
# After deployment:
# 1. Visit /api/health to verify deployment
# 2. Login with: uvmarketsignal@gmail.com / Admin@123456
# 3. Change the admin password immediately!