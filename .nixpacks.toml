# Nixpacks Configuration for Railway Deployment
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = ["npm run railway:build"]

[phases.start]
cmds = ["npm start"]

[variables]
NODE_ENV = "production"
PORT = "3000"

[healthcheck]
path = "/api/health"
interval = 30
timeout = 10
retries = 3
